package com.startup.factory;

public class GenderFactory {
    /*public static Gender createGender(String genderId, String decsription){
       String genderID = GenericHelper.GenerateID();

        if(ValidationHelper.isEmptyOrNullString(decsription))
            return null;

        return new Gender.Builder()
                .setGenderId(genderId)
                .setGenderDescription(decsription)
                .build();
    }

     */
}
